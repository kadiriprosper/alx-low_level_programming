#include <stdio.h>
/**
 * main - prints out number combo
 *
 * Return: 0
 */
int main(void)
{
int a = 48;
int b = 48;
int c = 48;
int d = 48;
while(a < 58)
{
	putchar(a);
	putchar(b);
	putchar(' ')
	while(c < 58)
	{
		putchar(c);
		putchar(d);
		d++
		if (d == 
	}
}
}
