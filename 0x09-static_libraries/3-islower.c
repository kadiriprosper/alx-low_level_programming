#include "main.h"
/**
 * _islower - prints out the alphabet
 *
 * @c: the character to be printed
 * Return: 0
 */
int _islower(int c)
{
if (c >= 97 && c <= 122)
{
return (1);
}
else
{
return (0);
}
}
